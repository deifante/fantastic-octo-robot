[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
fastapi = ">=0.67.0"
uvicorn = ">=0.14.0"

[dev-packages]
pytest = ">=6.2.5"
requests = ">=2.26.0"
Faker = ">=8.12.1"
black = "*"

[requires]
python_version = "3.8"

[pipenv]
allow_prereleases = true
